apiVersion: v1
kind: Service
metadata:
  name: llamacpp
  labels:
    app: llamacpp
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for direct pod access
  selector:
    app: llamacpp
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
